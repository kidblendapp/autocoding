/**
 * Global test setup
 * This file runs before all tests
 */

import { beforeAll, afterAll } from 'vitest';

// Global setup
beforeAll(() => {
  // Suppress console output during tests if needed
  // Can be configured per test file
});

afterAll(() => {
  // Global cleanup if needed
});
